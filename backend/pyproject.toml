[project]
name = "agentic-customer-care"
version = "1.0.0"
description = "Autonomous multi-agent customer care system for e-commerce powered by LLMs"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
